<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/White"
    android:orientation="horizontal"
    android:descendantFocusability="blocksDescendants"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageView
        android:id="@+id/imgAvatar"
        android:layout_width="@dimen/contact.imgview.avatar.width"
        android:layout_height="@dimen/contact.imgview.avatar.Height"
        android:scaleType="centerInside"
        android:src="@drawable/ic_avt1" />

    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="@dimen/contact.textview.username.margin_left"
        android:layout_weight="1"
        android:text="Brunch this week"
        android:textColor="@color/contact.textview.username.text_color"
        android:textSize="15sp" />

    <ImageButton

        android:id="@+id/imgBtnEdit"
        android:layout_width="@dimen/contact.imgbutton.width"
        android:layout_height="@dimen/contact.imgbutton.height"
        android:layout_gravity="center_vertical"
        android:background="@android:color/transparent"
        android:src="@drawable/bg_pressed_edit_button" />

    <ImageButton
        android:id="@+id/imgBtnDelete"
        android:layout_width="@dimen/contact.imgbutton.width"
        android:layout_height="@dimen/contact.imgbutton.height"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="@dimen/contact.deletebutton.margin_right"

        android:background="@android:color/transparent"
        android:src="@drawable/bg_pressed_delete_button" />

</LinearLayout>
