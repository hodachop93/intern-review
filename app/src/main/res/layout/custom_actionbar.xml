<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/actionbar.height"
    android:background="@color/actionbar.background"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@color/White"
        android:textSize="@dimen/actionbar.title.textsize" />

    <ImageView
        android:id="@+id/imgLeft"
        android:layout_width="@dimen/actionbar.height"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:scaleType="center"
        android:background="@drawable/bg_pressed_turquoise"/>

    <ImageView
        android:id="@+id/imgRight"
        android:layout_width="@dimen/actionbar.height"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:scaleType="center"
        android:layout_alignParentRight="true"
        android:background="@drawable/bg_pressed_turquoise"/>

</RelativeLayout>
